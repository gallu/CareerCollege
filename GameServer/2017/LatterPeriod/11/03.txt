// イニシアチブ判定(不変の時)
while(true) { //「１戦闘」のループ
    // イニシアチブ判定(１戦闘毎に変わるとき)
    // 「１ターン」にかかるエフェクトの処理
    while(true) { // 「１キャラクタ」のループ
        while(true) { // 「１行動」のループ
        }
    }
    // 追加行動
    while(true) { // 「追加行動する」キャラクタ
        while(true) { // 「１行動」のループ
        }
    }
}
